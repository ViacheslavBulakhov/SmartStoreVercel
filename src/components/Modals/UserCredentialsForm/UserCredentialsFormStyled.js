import styled from 'styled-components';

export const UserCredentialsFormWrap = styled.div`
  position: absolute;
  top: 0;
  left: 0;

  width: 100%;
  height: 100%;

  background-color: rgba(0, 0, 0, 0.8);

  border-radius: 20px;

  display: grid;
  place-items: center;
`;
